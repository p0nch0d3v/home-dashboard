version: '3'

services:
  home-dashboard:
    #restart: unless-stopped
    image: node:lts
    command: >
      bash -c "cd /app &&
               npm install &&
               npm run start"
    volumes:
      - .:/app
    ports:
      - 3000:3000
